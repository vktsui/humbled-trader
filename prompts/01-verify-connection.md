Use tv_health_check and tell me if TradingView is connected.

Expected: `cdp_connected: true` and `api_available: true`.

If false, run:

```bash
./scripts/launch_tradingview.sh launch
```

Then retry.
